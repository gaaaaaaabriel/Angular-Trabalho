// src/app/models/models.ts

// Definindo a interface para Categoria
export interface Categoria {
    nome: string;
    ativo: boolean;
  }
  
  // Definindo a interface para Estado
  export interface Estado {
    nome: string;
    sigla: string;
    ativo: boolean;
  }
  